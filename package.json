{
  "name": "eventorganizer",
  "version": "1.0.0",
  "description": "package for both",
  "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" ",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "joi": "^17.11.0",
    "nodemon": "^3.0.2"
  }
}
